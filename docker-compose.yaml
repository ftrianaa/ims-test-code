services:
  mysql:
    image: mysql:8.0
    container_name: ims
    restart: always
    env_file:
      - .env.production
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
